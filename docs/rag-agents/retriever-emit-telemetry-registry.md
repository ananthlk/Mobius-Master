# Retriever — Cross-Module Emit & Telemetry Registry

**Owner:** Retriever agent — same accountability gap class as the metrics scoreboard (Ananth caught: process tracking existed, cross-module rollups didn't). This one covers emit keys, DB schema asks, and narrative-layer wiring instead of latency/accuracy.
**Purpose:** one place to confirm no naming collisions, no conflicting DB schema asks, and no silently-assumed wiring between modules' narrative layers — a rollup TECH/UX can check, not something reconstructed per-module.
**Status:** STARTED 2026-07-23, immediately found one real unresolved conflict (see below) rather than a clean bill of health.

---

## Emit keys (response payload)

| Key | Owner | Scope | Collision-checked against |
|---|---|---|---|
| `gate` | Pre-existing, strategy-e fail-fast check | `{passed, fail_fast_reason}` | N/A — legacy field, do not touch |
| `shape_gate` | Shape:Gate | contour, d/j/p codes, probe, underspecified sub-object, reason, gate_ms | ✅ Confirmed distinct from `gate` (the naming collision Gate's UX review caught and avoided) |
| `shape_reformat` | Shape:Reformat | mirrors `shape_gate`'s shape (per Reformat's tracker: "Emit key `shape_reformat` (mirrors `shape_gate`)") | ✅ Confirmed distinct from both `gate` and `shape_gate` |
| `shape_structure` | Shape:Structure | `ResourcePosture {breadth, confidence_bar, max_attempts, speed_budget}` + `posture` + `rewritten_queries` — Diagnostics-only, NOT `thinking_trace` (Structure has no narrative layer, UX sign-off 2026-07-23) | ✅ Confirmed distinct from `gate`/`shape_gate`/`shape_reformat`. Built, tested (19/19), wired into orchestrator.py by Retriever, commit `1636c81` |
| `pool` | Pool (Step 2) | `segment_ms {doc_narrow_ms, tag_select_ms, embed_ms, vector_ms, inherited_ms, dedup_ms, neighbor_ms}` + `strategy_hint` + `fallback_triggered` + `pool_ms` — Diagnostics-only, no narrative layer (UX sign-off 2026-07-23, same pattern as `shape_reformat`) | ✅ Confirmed distinct from `gate`/`shape_gate`/`shape_reformat`/`shape_structure`. **FIXED 2026-07-23 — Ananth caught this had been flagged but never actually closed:** `orchestrator.py` now calls `run_slots()` + `run_pool_for_query()`/`run_pool_fanout()` after Structure, returning `pool: list[PoolResult]` + `pool_ms` on `RetrieverPartialResult`. Verified live: `python -c "from app.services.retriever.orchestrator import run_retriever_partial"` imports cleanly. `PoolResult.segment_ms` now has a real caller — no longer an unreachable return value. Still Diagnostics-only per UX's ruling; nothing here feeds `narrative`/`thinking_trace` (Pool has no `narrate()` layer, correctly, per the row below). |
| `synthesis` | Synthesis (Step 5) | `SynthesisTelemetry {chunks_in, chunks_out, duplicates_removed, neighbors_added, neighbors_skipped_no_anchor, unverified_citations, planned_status_citations, document_name_resolved, document_name_fallback, document_name_lookup_missed, citations_trimmed_for_budget, fusion_dropped_redundant, fusion_dropped_budget, fusion_redundant_detected_not_dropped, per_slot_verdict, per_slot_ride_along, compile_ms, segment_ms}` + `SynthesisResult.coverage_diagnostics: dict[slot_id, CoverageDiagnostic]` — Diagnostics-only, no narrative layer, same posture as `pool` | ✅ Confirmed distinct from all rows above. **UPDATED 2026-07-24 — LIVE in `orchestrator.py`'s production path, 9/9 sign-offs closed + Technical Review PASS.** Synthesis does NOT author answer text (superseding this module's original "text + citations" kickoff framing) — it compiles Fillers' FilledShape into a reranked/deduped/neighbor-complete/cited package (`app/services/retriever/synthesis.py`'s `compile_synthesis()`, contracts in `synthesis_contracts.py`) that Chat authors the answer from and Eval grades against. **Blend model (2026-07-24, same day as production wiring): per-slot rerank replaced with real cross-strategy fusion** — retention (orchestrator) unions every executed rung's chunks into a slot instead of discarding prior rungs; Synthesis groups by strategy (derived from `assignment_reason`), fuses with Reciprocal Rank Fusion (`fusion.py:rrf_fuse`), and selects to a per-slot token sub-budget with Maximal Marginal Relevance (`fusion.py:mmr_select`, TF-IDF cosine, λ=0.7, redundancy threshold 0.85) — a real blended fill, not winner-takes-all. `fusion_dropped_redundant`/`fusion_dropped_budget` are the two new fusion-level drop-path counters this adds, distinct from the pre-existing `citations_trimmed_for_budget` (a later, separate global safety-net trim after neighbor completion). `coverage_diagnostics` is one `CoverageDiagnostic` per slot (including empty ones — Router needs one per slot, ambiguous otherwise): `pool_verdict` (`gaps_remain`/`saturated`/`budget_full`), `reason` (verbatim, never normalized), `saturated_strategies` (corrected definition, 2026-07-24: "≥1 candidate rejected as redundant," not "all rejected" — the original was proven mathematically unreachable), `per_strategy_counts`. Router's `decide_continuation()` consumes this on the next loop iteration. `document_name_resolved`/`document_name_fallback`/`document_name_lookup_missed` track a real upstream gap: neither `PoolCandidate` nor `FilledChunk` carries a document display name anywhere before this step — resolved here via a batched `rag_published_embeddings` lookup, with an id/url-derived fallback when it misses. A technical failure of either DB-touching call RE-RAISES after one local retry (Retriever's 3-layer retry ruling) rather than degrading to a soft error counter — `compile_synthesis` propagates to the orchestrator's whole-loop retry. `segment_ms` (DB's ask, 2026-07-24) breaks `compile_ms`'s total into `{rerank_ms, neighbor_completion_ms, dedup_ms, name_resolution_ms, citation_build_ms}`, same convention as `pool`'s `segment_ms` — Synthesis makes no LLM calls and does no citation-verification/grounding-check itself, so those aren't segments here. `chunks_out` reconciles exactly to `chunks_in − fusion_dropped_redundant − fusion_dropped_budget − duplicates_removed + neighbors_added` (Eval's ask, formula extended for fusion), logged as an ERROR if it ever doesn't. **Data-collection pullback (2026-07-24, same day, blend-model-design.md §11): the blend is DEACTIVATED (allocator weight 0) pending real recall-at-K curves, not deleted** — `compile_synthesis` gained an explicit `data_collection_mode: bool = False` param (default off, every existing caller unaffected). When `True`, MMR's real redundancy-drop/budget-cutoff is skipped per slot (a forced single strategy's true top-X must reach Chat/Eval uncontaminated by MMR's still-uncalibrated λ/threshold) — MMR still runs once per slot as a probe, feeding `fusion_redundant_detected_not_dropped` (new counter, log-only near-duplicate density, never subtracted from `chunks_out`/the reconciliation identity since nothing is actually dropped) and a genuine `budget_full`/`gaps_remain` split on `coverage_diagnostics` without applying it. Deliberately gated on an explicit caller flag, not on "this slot's chunks happen to share one strategy" — that shape occurs organically in normal blend-mode operation too and must still get real per-slot MMR budget-splitting there (a regression caught by the existing test suite when first implemented as shape-inferred). `per_slot_ride_along` carries Eval's non-negotiable attribution field through. One `FilledChunk` contract-change ask still pending with DB (`title: str | None` for DDG-sourced external titles) — `quote_verified: bool | None` landed and is fully live end-to-end. **Real crash bug found and fixed 2026-07-24 (Retriever)**: filler_s's `fact_store` chunks have a synthetic (non-UUID) `chunk_id`/`document_id` that the old internal/external binary misclassified as a real corpus document, sending it into a `CAST(:ids AS uuid[])` query and crashing every `s`-sourced answer — now excluded from both name-lookup and neighbor-completion eligibility, and correctly labeled `authority="authoritative"` (was mislabeled `"external"` by the same root cause). |
| `filled_shape.emit` | Retriever (`orchestrator.py`'s `_run_fillers_simple`, the multi-turn execution loop) | `{slots_filled, under_filled, final_verdicts: {slot_id: verdict}, ride_along_slots: [slot_id], executed_order: {slot_id: [strategies run, in order]}, prescreen_not_ready_deferred_slots: [slot_id], attempt_spans: {slot_id: [{strategy, attempt_number, t_attempt_start_ms, t_attempt_end_ms}]}}` — Diagnostics-only, no narrative layer | ✅ Confirmed distinct from all rows above. **NEW 2026-07-24.** `final_verdicts`/`ride_along_slots` reflect the STOPGAP verdict function (bare occupancy check) today, not Observer's real per-strategy logic — will read Observer's real verdicts once production wiring clears the build-gate, same key names, no consumer changes needed. `executed_order` and `prescreen_not_ready_deferred_slots` are Router's required boundaries (router-build-spec.md §11) for the readiness-aware "d" deferral: the former proves executed order is emitted as executed, the latter is the non-negotiable distinct label so a slot where "d" got deferred and a sibling strategy succeeded instead reads as that strategy's real success, not a folded-in "d" failure that would corrupt Eval's calibration cell for "d". **`attempt_spans` added 2026-07-24 (Timing, Step 7's one concrete gap, module-gates.md §7): "escalation loop must emit per-attempt timing (t_attempt_start, t_attempt_end) before shape can be signed off"** — offsets are relative to the loop's own `t0` (monotonic), not wall-clock epoch, same convention as `pool`/`synthesis`'s `segment_ms`; one entry per rung actually executed on a slot, in execution order, across both turn-0 and the multi-turn continuation loop. |
| `contract` | Retriever (`app/services/retriever/contract.py`'s `build_contract()`) | Frozen 12-field envelope: `{query, chosen_slot, score, chunks[], answer_text, thinking, traces, routing_keys, grounding_markers, latency_ms, attempt_count, status}` (module-gates.md §6) | ✅ **NEW 2026-07-24, v1 built, 14/14 tests (`test_contract.py`).** One emitter — every pipeline exit path threads through `build_contract()`. Built against `module-gates.md`'s 12-field list, not `retriever-build-checklist.md`'s conflicting 13-field draft (stale, legacy `routing_dump`/`feature_vector` concepts) — flagged as a doc inconsistency worth reconciling, not silently picked one and moved on. `answer_text`/`thinking` are optional, caller-supplied params (default `None`) — Chat authors these downstream, not Synthesis (Ananth's correction superseded the original module-gates.md assumption that Synthesis produces them); this envelope is commonly built BEFORE Chat has run, and `None` there is the honest, expected value, not an unfilled stub. `chunks` is `SynthesisResult.citations` flattened to dicts (empty list if Synthesis hasn't run — same pre-wiring degrade posture as Observer/Synthesis). Not yet wired into `orchestrator.py`'s return path or cross-agent signed off. |

## rag_query_decisions column asks (lean scalars — decision-row stays index-lean)

| Column | Requested by | Status |
|---|---|---|
| `gate_contour` | Shape:Gate (UX recommendation) | Requested — **migration status unconfirmed**, need to verify with DB whether this actually landed or is still "recommended" |
| `gate_underspecified_kind` | Shape:Gate (UX recommendation) | Same as above |
| `reformat_posture` | Shape:Reformat (UX recommendation, "mirrors `gate_contour`") | Requested, part of the package DB is currently reviewing (prevalence-query pattern + these columns) — **not yet signed off** |
| `reformat_fanout_n` | Shape:Reformat (UX recommendation) | Same as above |

**Open verification task:** none of these 4 columns have been confirmed as actually migrated into the live schema — all 4 are "recommended by UX, in DB's review queue." Don't let TECH assume any of this is live without checking.

## Narrative layer wiring — thinking_trace (UNRESOLVED CONFLICT FOUND 2026-07-23)

| Module | `narrate()` intent (per own docstring) | UX sign-off actually says | Status |
|---|---|---|---|
| Shape:Gate | Chat-bubble register, default-visible | **Explicitly confirmed**: "Wire into the 12-field contract's `thinking_trace` slot — that's exactly what it was reserved for" (`gate-emit-schema-spec.md`) | ✅ Resolved |
| Shape:Reformat | Chat-bubble register, default-visible (`reformat_narrate.py`'s own docstring, same design intent as Gate's) | **RESOLVED by UX 2026-07-23**: `shape_reformat` (the structural telemetry key: posture/fanout_themes/latency_ms) is a DIFFERENT layer from `narrate()` (the narrative function) — the former stays backend/Diagnostics-only, the latter IS user-facing, same as Gate's. Both feed `thinking_trace`, concatenated in order (Gate first, Reformat second), but **only for postures where Reformat adds real information**: FAN_OUT/RELY_ON_EXTERNAL/CLARIFY. PRECISE/DECLINE/CLARIFY_REPHRASE use Gate's narration alone (Reformat's would be redundant). | ✅ **Resolved, implemented in `orchestrator.py` (commit `28b98d0`), verified live on both branch types** |

**Implementation, live:** `orchestrator.py`'s `_include_reformat_narration(posture)` gates whether Reformat's `narrate()` output is appended. Verified on 2 real queries: a PRECISE case correctly shows Gate's narration only; a RELY_ON_EXTERNAL case correctly shows both, concatenated with a blank line. Note: the combined field is still named `narrative`/`narrative_full` in `RetrieverPartialResult` — NOT `thinking_trace` (that name only exists as a comment/intent marker; the real 12-field `thinking_trace` slot lives on the eventual CONTRACT module, Step 6, which doesn't exist yet). Whoever builds Contract needs to know this composition rule, not just Structure.

## PHI handling (fail-closed rule, per module)

| Module | Rule | Verified how |
|---|---|---|
| Shape:Gate | `narrate_full()` never persisted (compute on-demand for Diagnostics only, discard after render) | Confirmed in code docstring + TECH's independent review |
| Shape:Reformat | Same rule, per `reformat_narrate.py`'s own module docstring ("per Gate's PHI rule... NEVER persist this output") | ✅ **Independently verified 2026-07-23** — grepped both `reformat.py` and `reformat_narrate.py` for any persistence call (INSERT/`.save()`) touching `narrate_full()` output. Zero found. Matches Gate's standard. |
| Pool | No narrative layer exists (Diagnostics-only `segment_ms`, no `narrate()`/`narrate_full()` equivalent) — candidate `text` fields carry raw corpus content, PHI risk if ever logged/persisted | ✅ **Verified 2026-07-23** — grepped all of `app/services/retriever/pool/*.py` for any `logger.`/`_log_stage`/`.save(`/`INSERT INTO`/persist call. **Zero found.** Nothing in Pool logs or persists candidate text today — consistent with the fail-closed rule, but also means there is no telemetry sink at all yet (see `pool` emit key row above), not just a PHI-safe one. |

## `rag_query_decisions` column migration status — CHECKED LIVE 2026-07-23

Queried `information_schema.columns` directly: **none of the 4 columns exist in the live schema.** This is expected, not a dropped ball — DB's own Reformat sign-off explicitly deferred this DDL until Router exists, to migrate read+write together in one combined pass rather than twice. Correctly gated on Router's future build, not something to chase now.

---

## Resolved items (closed 2026-07-23)

1. ~~Resolve the thinking_trace conflict~~ — ✅ UX ruled, implemented, verified (see above).
2. ~~Verify the 4 rag_query_decisions column asks' actual migration status~~ — ✅ checked live, confirmed not-yet-migrated by design (Router-gated).
3. ~~Independently verify Reformat's PHI fail-closed claim~~ — ✅ verified, zero persistence calls found.

## Open items

1. **This registry is a standing habit**, same as the metrics scoreboard — update as each module reports, don't reconstruct after the fact.
