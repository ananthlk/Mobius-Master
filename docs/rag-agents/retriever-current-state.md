# Retriever — Current-State Assignment (the `corpus_search_*` tangle → 7 future modules)

**Status:** draft 2026-07-22, by **Retriever** (actor). For TECH to fill the *current* column of `reports/techday/retriever-alignment-board.html`. **Current-state only — no build, no code moves.**
**Method:** verified in code today, NOT copied from the spec. The spec's §8/§9 anchors have **drifted** (audit was pre-P1); every line below re-checked against live `mobius-rag/app/services/*`. Deltas flagged ⚠️.

## Repo map (13,166 LOC across 7 files — matches spec "~13k")
| file | LOC | role today |
|---|---|---|
| `corpus_search_agent.py` | 5,643 | the core tangle — entry, escalation loop, dispatch, observe/INSERT, envelope |
| `corpus_search.py` | 3,826 | low-level retrieval — pool build internals, `_bm25_arm`, vector/keyword scans |
| `corpus_search_strategy_c.py` | 1,139 | filler **c** impl |
| `corpus_search_strategy_d.py` | 979 | filler **d** impl |
| `corpus_search_router.py` | 862 | **v1 router** (serves; `decide()`, `_LINEAR_BASE/_WEIGHTS`, `RouteDecision`) |
| `corpus_search_router_v2.py` | 283 | **v2 router** — imports v1 (`:24`), `decide()` `:143`; behind `ROUTER_VERSION=v2` |
| `corpus_search_lexicon.py` | 434 | query-time lexicon **lookup** (mine; build/tagging = Curation) |

**Public entry:** `corpus_search_agent()` `agent.py:3066` → `_corpus_search_agent_impl()` `:3766`.

## The 7 future modules → where each concern lives TODAY

| Future module | Current location(s) — file:line | What it is today | Notes / ⚠️ deltas |
|---|---|---|---|
| **pool** | `agent.py: build_candidate_pool() :1762`; pre-route build `:3995-4036` (`partition_pre :4006`, `pool_pre :4008`, inheritance augment `:4015`); reuse `:4784`; `corpus_search.py: _bm25_arm() :806` | Candidate pool built by `build_candidate_pool`; BM25 arm is separate in `corpus_search.py`. Pool built once per `_impl` call (`:4784` reuses the pre-route `pool_pre`, **intra-call only**). | ⚠️ **The "double-build" is ONE arm-level redundancy in `corpus_search.py`:** `_bm25_arm` runs strict `:3347` then a **conditional relaxed re-query** `:3417` when strict under-fills — a second *keyword fetch*, not two full pool builds (DB confirmed). ⚠️ `_domain_fallback_pool() :2040` = **DEAD (0 callers)**. ⚠️ **Cross-attempt rebuild (spec L5 — verified real but rare):** loop `:3137` re-invokes `_impl` per attempt (`:3145`) via a `model_copy` carrying **no pool** (`:3141`); `pool_pre` is local, rebuilt each call (guard `:4003`) → pool **is** rebuilt per escalation attempt (no cross-attempt memo). BUT escalation is rare (budget=1, abstain-gated, fast-exit `:3174`) → a normal trace shows single-build (what DB measured). Both regimes true. |
| **shape** | `_MAX_TRIES=4 :3095`; escalation loop `for _attempt in range(_MAX_TRIES) :3137`; `_get_escalation_budget() :2321`, budget `:3116`, done-counter `:3117`; synthesis-abstain trigger `_is_synthesis_abstain :3273` / escalate `:3279`; `answer_shape` field `:2939` | The a→b escalation ladder = an attempt loop bounded by `_MAX_TRIES` + an escalation budget that fires **on synthesis abstain**. `answer_shape` exists only as a hint string (`essay/structured/binary/any`). | ⚠️ **No realized AnswerShape slot model yet** — code comment `:24` "NO external escalation yet". "Shape" today = the escalation-budget loop, not a slot engine. This is the module with the most future/current gap. |
| **fillers (a/b/s)** | dispatch if-chain in impl: `b :4390`, `c :4500` (imports `strategy_c`), `d :4633` (imports `strategy_d`), `e :4342`; params `_STRATEGY_PARAMS :2179`/applied `:5060`; **a** = BM25 fallthrough via `corpus_search._bm25_arm`; **s** = pre-route short-circuit returning `strategy_chain=["s"]` `:3929/3943/3961` | Arms are NOT pure over a shared pool today — `strategy_c/d` open their own scans/embeds (own files). `a` = keyword/BM25 over the pool. `s` = short-circuit, no routing. | ⚠️ **c/d→a redirect** now at `:4494-4497` (spec said :4405). Distinct arms the union spans = **{a,b,s}** (c/d redirect to a under `skip_synthesis`). This inversion (fillers own pools) is the core refactor target. |
| **router** | select `:59-72` (v1 `_router_decide_v1` default; v2 iff `ROUTER_VERSION==v2` `:69-70`); call sites `:4161`, override `:4166`, `:4170`; `explicit_strategy :4147` | ONE effective router: **v1 serves**, v2 is a layer importing v1 (`router_v2.py:24`). `decide()`/`decide_override()`. | ✅ Confirms board: v1 serves, no dead router — consolidation is a MERGE not a delete. `RouteDecision.invoke_all` added to v1's dataclass. |
| **synthesis** | `_synthesize_internal_answer() :2673`; brief-answer helper `:2640`; multi-invoke union synth `:4291`; abstain detection `:3273` | One synth entry (`_synthesize_internal_answer`), reached from multiple paths (single-arm, multi-invoke union). Abstain feeds the shape escalation. | Multi-path callers into one synth fn — future "one synthesis pass" is close but reached from ≥3 sites. |
| **contract** | `routing_dump` built `:4200-4263`; threaded to return sites `:4322,4351,4464,4580,4720,4975,5546` (~8, matches spec); `_persist_routing_decision_async() :3402`; `_observe_async() :3490`; **INSERT `rag_query_decisions` :3606**; leaf_key `:3578-3582`, feature_vector `:3584-3586`, params `:3631-3632`; `_with_chain() :3197`; trace INSERT `rag_query_traces :3737` | The 12-field envelope = `routing_dump` projected once, threaded to 8 returns. Decision-row written by `_observe_async`. | ⚠️ Anchors drifted (routing_dump :4200 not :4111; observe :3490 not :3539). ⚠️ **s-row NULL feature_vector is now EMERGENT**, not an explicit s-branch: `_feature_vector = _routing.get(...) or None :3586` + `... if _feature_vector else None :3632`. The null-guard IS the s-contamination protection — a shape-loop that populates `_routing['feature_vector']` for s would silently break the contract edge. |
| **timing** | ✅ timed: pre-route span `[trace:pre_route_timing] :4037` (partition/pool/inherited/recall_ms); leaf retrieval SQL in `corpus_search.py` (`embed_ms/bm25_ms/vec_ms/rerank_ms :3298-3411`, persisted `:3222-3255`); strategy_b `:571-866`; literal-probe `:2115-2175`. ❌ untimed: escalation loop / answer-path orchestration `:3137-3399` (only global `t0 :3778`); dtag `?` counts; c/d→a redirect re-runs; union assembly | **PARTIAL, inconsistent** — leaf-SQL + pre-route ARE spanned, but the orchestration layer that calls the arms 1–4× is not. | ⚠️ **Reconciles two lenses:** spec §8 L0 "untimed pre-route" is CLOSED (pre-route span landed `:4037`), so my first pass over-stated "mostly timed"; DB agent's "segments untimed / latency-blind" is right about the **orchestration layer** (loop attempts, dtag, redirect re-runs, union). End-to-end attribution still needs the missing loop-level spans → gate (d) target. |

## Structural-gate current-state (spec §4 f/g/h — verified where noted)
- **ONE-WRITER (f):** ⚠️ **TWO** `INSERT INTO rag_query_decisions` sites today — `agent.py:3606` (prod observe) + `eval/calibrate.py:362` (eval). Matches spec's warning; a 3rd post-refactor = RED.
- **ONE-ROUTER (c):** ✅ v1 serves, v2 imports v1 — no forked second decision path.
- **ONE-IMPORTER / VERSION-PIN (g/h):** `check_facts` / `FACT_CHECKER_VERSION` — Eval-owned Tier-2; not re-audited here (their gate). Flagged for the align step.

## Headline deltas the alignment should absorb
1. Timing is **PARTIAL, not "closed" and not "fully untimed"**: pre-route span + leaf-SQL ms ARE emitted; the **escalation/answer-path orchestration is untimed** (why the latency probe was blind). Gate (d) target = span the loop-level segments, not re-add pre-route.
2. Pool double-build is in **`corpus_search.py` (`_bm25_arm` ×2)**, not the agent — the pool module spans both files.
3. `_domain_fallback_pool :2040` is **dead** — SAFE-DELETE, verified 0 callers.
4. **shape** has the largest current→future gap (no slot model yet; just the escalation-budget loop).
5. Contract's **s-row NULL feature_vector is emergent from a null-guard**, not an explicit branch — fragile; the pool/shape rebuild must preserve it.

## DB-layer current-state (verified live against `mobius_rag` by DB agent / sad-nobel; index claims re-checked in migrations by Retriever)
- **pool source** = `rag_published_embeddings`, **1,937,353 rows**. Read paths healthy: `embedding_vec vector(1536)` + HNSW cosine `rag_published_embeddings_vec_hnsw` (**0 NULL vecs**); `search_vec tsvector` + GIN `rag_published_fts_gin`.
  - ⚠️ **`chunk_{d,p,j}_tags` have NO GIN index** (verified: `add_chunk_tags_to_published.py` adds the cols; only `document_tags` + FTS get GIN) → the tag arm's `?` predicate **seq-scans 1.94M rows**. Ties to the tag-IDF/selectivity lever (per-tag row-count = seq-scan today). §10 future decision: **pool-first vs GIN** — DB gates, must be timed.
- **fillers — arm-owned DB access (current):** bm25 arm → FTS query; vector arm → embedder call + HNSW query; dtag arm → per-key IDF `?` counts. Each **opens its own conn and/or embeds** — the exact inversion the refactor fixes (`fill(pool, shape)`, no db handle).
- **router / decision-row:** `rag_query_decisions` — `feature_vector` + `strategy_scores` were silently NULL until **db8f597** (now populate). Two-writer split (`agent:3606` + `eval/calibrate.py:362`) is current reality; gate (f) collapses to one.
- **cross-cutting (DB's lane, NOT this cut):** service pinned **min=max=1** because job state is in-process — externalizing it is a **separate follow-on**, not the engine refactor. Prior answer-path refactor leaked **bare-BEGIN idle-in-transaction** txns → connection exhaustion → blocked Payor; guardrail `idle_in_transaction_session_timeout=120s` set permanently on `mobius_rag`. **Rebuild constraint:** pool/writer must ride the shared pool (no per-call connect), no bare txns.
- **DB sign-off:** conditional ✓ → full green once these land in current-state **and** the §10 conditions are in §4 (gate section = **TECH's** to fold, not mine).
