# Dev eval questions for retrieval module validation.
# Optional per question:
#   golden_answer: expected answer text (for display)
#   gold_ids: list of chunk/paragraph IDs that contain the answer (for "gold in top" checks)
#
# Run one question:
#   uv run python -m mobius_retriever.cli retrieval-report -q "What is the prior authorization requirement..." --jpd --bm25-only
#
# Run all (from project root):
#   for q in "What is the prior authorization..." "What phone number do pharmacies..." ...; do
#     uv run python -m mobius_retriever.cli retrieval-report -q "$q" --jpd --bm25-only
#   done

questions:
  - id: dev_001
    question: What is the prior authorization requirement for physical therapy in Florida?
    expect_in_manual: true
    # gold: parent_metadata_ids to add after inspection

  - id: dev_002
    question: What phone number do pharmacies call for Express Scripts help desk?
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  - id: dev_003
    question: Summarize the key guidance in the provider manual section "Medicaid in Florida".
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  - id: dev_004
    question: What is the 72-hour emergency medication supply policy?
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  - id: dev_005
    question: How can a provider determine whether a service requires prior authorization?
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  - id: dev_006
    question: What are the weekend and after-hours on-call phone numbers?
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  - id: dev_007
    question: Summarize the Secure Provider Portal registration process.
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  - id: dev_008
    question: What is the process for claims reconsiderations and disputes?
    expect_in_manual: true
    # gold: parent_metadata_ids to add

  # Out-of-syllabus probe (not in Sunshine provider manual)
  - id: dev_009
    question: What is the Medicare Part B prior authorization process in California?
    expect_in_manual: false
