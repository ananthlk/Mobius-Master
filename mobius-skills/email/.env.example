# System sender
MOBIUS_EMAIL_FROM=mobiushealthai@gmail.com

# Gmail API (OAuth2): path to credentials.json from Google Cloud Console
GMAIL_CREDENTIALS_PATH=./credentials.json
# Optional: path where to store/load OAuth token (default: ./token.json)
# GMAIL_OAUTH_TOKEN_PATH=./token.json

# SMTP fallback (Gmail app password) - use if not using Gmail API
# GMAIL_APP_PASSWORD=xxxx
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# LLM for crafting subject/body (optional)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# Or Vertex:
# VERTEX_PROJECT_ID=...
# GOOGLE_APPLICATION_CREDENTIALS=path/to/sa.json
# VERTEX_LOCATION=us-central1
# VERTEX_MODEL=gemini-1.5-flash

# Limits (optional)
# EMAIL_MAX_SUBJECT_LEN=500
# EMAIL_MAX_BODY_LEN=100000
